Python Notes:

jupyter notebook Essentials ( Markdown )
    # My Main Title
    ## Major Section
    ### Sub-section
    #### Further Detail

shift + Enter - This shortcut executes the current code cell and then automatically selects the next cell below it
Alt + Enter   - This shortcut executes the current code cell and then inserts a new empty code cell immediately below the executed cell, placing the cursor in this newly inserted cell.
shift + Tab   - To view function parameters 

Install Pandas

conda install pandas
pip install pandas